- name: Configure Managed Hosts
  hosts: all
  gather_facts: false

  tasks:
    - name: Debug
      ansible.builtin.debug:
        msg: "Checking connectivity"
    - name: Sleep
      ansible.builtin.pause:
        minutes: 5
